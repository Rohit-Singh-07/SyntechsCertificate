/**
 * Single source of truth for the public site URL.
 *
 * Update this ONE value after deploying to your final domain.
 * Every certificate QR code and verification link is built from it.
 *
 * - No trailing slash.
 * - Must include the protocol (https://).
 */
export const SITE_URL = "https://syntechs-certificate.tixin.in/";
